import { ToggleThemeDirective } from './toggle-theme.directive';

describe('ToggleThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleThemeDirective();
    expect(directive).toBeTruthy();
  });
});
