import { SideMenuToggleDirective } from './side-menu-toggle.directive';

describe('SideMenuToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new SideMenuToggleDirective();
    expect(directive).toBeTruthy();
  });
});
