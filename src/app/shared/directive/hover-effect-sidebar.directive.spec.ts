import { HoverEffectSidebarDirective } from './hover-effect-sidebar.directive';

describe('HoverEffectSidebarDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverEffectSidebarDirective();
    expect(directive).toBeTruthy();
  });
});
